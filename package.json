{
  "name": "vzou",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && yarn start",
    "frontend-build": "cd frontend && npm run build",
    "frontend-start": "cd frontend && npm run release"
  }
}
